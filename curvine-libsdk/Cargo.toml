[package]
name = "curvine-libsdk"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["rlib", "cdylib"]
name = "curvine_libsdk"

[features]
default = ["java-sdk", "opendal-s3"]
rust-sdk = []
java-sdk = ["dep:jni"]
python-sdk = ["dep:pyo3"]

# Forward optional UFS backends from curvine-client so Java/Python/Rust
# consumers can enable them without depending on curvine-client directly.
# The default SDK build keeps the historical OpenDAL S3 backend. Consumers that
# need a lighter SDK can use `default-features = false` and opt in explicitly.
opendal-s3 = ["curvine-client/opendal-s3"]
opendal-oss = ["curvine-client/opendal-oss"]
opendal-gcs = ["curvine-client/opendal-gcs"]
opendal-azblob = ["curvine-client/opendal-azblob"]
opendal-cos = ["curvine-client/opendal-cos"]
opendal-hdfs = ["curvine-client/opendal-hdfs"]
opendal-webhdfs = ["curvine-client/opendal-webhdfs"]
opendal-hdfs-native = ["curvine-client/opendal-hdfs-native"]
oss-hdfs = ["curvine-client/oss-hdfs"]

[dependencies]
orpc = { workspace = true }
curvine-client = { workspace = true, features = ["job-client", "unified"] }
curvine-common = { workspace = true }
curvine-config = { workspace = true }
once_cell = { workspace = true }
tracing = { workspace = true }
tracing-appender = { workspace = true }
tracing-subscriber = { workspace = true }
log = { workspace = true }
tokio = { workspace = true }
futures =  { workspace = true }
bytes = { workspace = true }
pin-project-lite = { workspace = true }
dashmap = { workspace = true }
toml = { workspace = true }
serde = { workspace = true }
prost = { workspace = true }
jni = { version = "0.21.1", optional = true }
pyo3 = { version = "0.25.1", features = ["extension-module","abi3-py38","num-complex"], optional = true }
